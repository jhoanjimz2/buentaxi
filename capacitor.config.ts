import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gov.co.santamarta.buentaxiapp',
  appName: 'BuenTaxi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
